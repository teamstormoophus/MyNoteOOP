<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hulk.pandaminder">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_panda"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_panda"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.todo.TodoHouseActivity" />
        <activity android:name=".ui.todo.TodoListActivity" />
        <activity android:name=".ui.notes.NoteActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>